# /deepseek

Expand this command to the same request as:

```text
$deepseek {args}
```

The `deepseek` skill is the single source of truth for delegation policy. Do
not duplicate the delegation rules in slash-command handling.
